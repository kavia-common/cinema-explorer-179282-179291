# React App Environment Variables

# Supabase
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=

# TMDB (The Movie Database)
# You can use either:
# - A v3 API key (32-char), which will be sent via ?api_key=...
# - OR a v4 Access Token (JWT string starting with 'eyJ'), which will be sent via Authorization: Bearer ...
# The client auto-detects which one you provided.
REACT_APP_TMDB_API_KEY=
